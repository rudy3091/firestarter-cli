export const main = `#include <iostream>`;

export const cstdio = `#include <cstdio>`;
export const cmath = `#include <cmath>`;

export const vector = `#include <vector>`;
export const set = `#include <set>`;
export const algorithm = `#include <algorithm>`;
export const queue = `#include <queue>`;
export const stack = `#include <stack>`;
export const map = `#include <map>`;
export const tuple = `#include <tuple>`;
